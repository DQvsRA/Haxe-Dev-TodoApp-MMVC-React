package consts;
class Defaults {

    public static var COMPONENT__FACTOR__WIDTH(default, never):Float = 0.618;
    public static var COMPONENT__FACTOR__HEADER(default, never):Float = 0.08;
    public static var COMPONENT__FACTOR__TODOLIST(default, never):Float = 0.88;
}
